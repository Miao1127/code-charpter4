重要：将局部协同搜索作为第一部分，首先进行区域内无障碍的搜索，然后再在区域内添加障碍物，进行区域协同搜索，
分区分配作为第二部分，搜索结果与第一部分做对比，再将局部协同搜索算法当作分区内的协同算法；
1.随机生成一个概率地图，边界栅格的目标存在概率为0，内部栅格概率随机生成，并用热力图表达此概率地图：（完成）
2.计算栅格信息熵，即对应航线上栅格的上一时刻的信息量与当前时刻的信息量的差值，需要确定的元素包括：栅格编号、上一时刻栅格概率值和当前时
  刻栅格概率值，要计算栅格编号，首先需要确定usv可以选择的航线分类以及航线长度，上一时刻栅格概率可以从usv存储的概率图中获得，当前时刻的
  的栅格概率值需要由观测信息和环境变化信息综合得到，得到当前时刻概率值后需要将此值更新到概率地图中，同时发送给其他usv；
  (1)usv可以选择的航线有三种，分别为直行、左转和右转，对应的选择概率为0.4、0.3和0.3；
  (2)随机生成初始搜索概率图，5*5的搜索栅格构成一个路径栅格；
  (3)计算上一时刻的搜索栅格信息量，首先需要确定搜索栅格范围，然后，再从搜索概率图中获得存储的对应栅格概率值；
  (4)计算当前时刻的搜索栅格信息量，首先

3.马尔科夫决策，计算最佳的价值函数
